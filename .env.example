# ═══════════════════════════════════════════════════════════════════════════
# CHARGE SCOLAIRE - Environment Variables
# ═══════════════════════════════════════════════════════════════════════════

# API Mode: 'mock' | 'real' | 'auto'
# - mock: Uses mock data (for development/demo)
# - real: Uses real École Directe API (requires proxy)
# - auto: Uses mock in development, real in production
VITE_API_MODE=mock

# Set to 'true' to force real API in development
# (requires the Vite proxy or EDP Unblock extension)
VITE_USE_REAL_API=false

# Custom proxy URL for production (optional)
# Only needed if you host your own proxy server
# VITE_PROXY_URL=https://your-proxy-server.com/api
